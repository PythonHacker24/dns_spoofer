# dns_spoofer

This is a python3 program which spoofs the DNS requests comming from the target.

!!!! ARP SPOOF THE TARGET FIRST !!!!

If the target tries to access the given domains, the scipt redirects it to the given IP Address regardless the respose of the DNS Server has made.
